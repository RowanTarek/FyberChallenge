<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
      <!--  <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>-->
        <item name="android:windowBackground">@android:color/white</item>

    </style>
    <style name="offerInfoBoxText" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/darkGrey</item>
        <item name="android:background">@drawable/oval_grey</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:paddingTop">@dimen/itemSmallPadding</item>
        <item name="android:paddingBottom">@dimen/itemSmallPadding</item>
        <item name="android:paddingRight">@dimen/itemMediumPadding</item>
        <item name="android:paddingLeft">@dimen/itemMediumPadding</item>
    </style>

</resources>
